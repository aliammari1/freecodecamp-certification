# exercise-tracker — copy to .env and fill in.
# MONGO_URI is required and validated at boot (znv); the app refuses to
# start without a valid mongodb:// connection string.
MONGO_URI=mongodb://localhost:27017/exercises
PORT=3000
